# Frontend demo mode (no backend required)
# Set to 1/true to use local demo data instead of calling backend.
VITE_DEMO_MODE=0

# When backend is available, keep using /api and /uploads via Vite proxy
VITE_API_BASE=
